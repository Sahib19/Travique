<nav class="navbar navbar-expand-lg sticky-top border-bottom">
    <div class="container-fluid">
        <!-- Brand/Logo Section -->
        <div class="nav-header">
            <div class="nav-logo">
                <a class="navbar-brand" href="/listing">
                    <i class="fa-solid fa-compass"></i>
                </a>
            </div>
            <div class="nav-name">Travique</div>
        </div>

        <!-- Mobile Toggle Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" 
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible Content -->
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <!-- Main Navigation Links -->
            <div class="navbar-nav me-auto">
                <a class="nav-ls-button" href="/listing">
                    <i class="fa-solid fa-plane-up"></i> Explore
                </a>
                <a class="nav-ls-button" href="/listing/new">
                    <i class="fa-solid fa-plus"></i> Add Destination
                </a>
            </div>

            <!-- Search Form -->
            <form method="POST" class="d-flex my-2 my-lg-0" role="search" action="/listing/search">
                <input class="form-control me-2 search-input" id="searchInput" type="search" name="placeName"
                    placeholder="Search destinations" aria-label="Search" />
                <button id="searchBtn" class="btn btn-outline-danger search-button" type="submit">
                    <i class="fa-solid fa-magnifying-glass search-icon"></i>Search
                </button>
            </form>

            <!-- User Authentication Links -->
            <div class="navbar-nav ms-lg-3">
                <% if(!currentUser) { %>
                    <a class="nav-ls-button" aria-current="page" href="/user/signup">
                        Signup <i class="fa-solid fa-user-plus"></i>
                    </a>
                    <a class="nav-ls-button" href="/user/login">
                        Login <i class="fa-solid fa-right-to-bracket"></i>
                    </a>
                <% } %>

                <% if(currentUser) { %>
                    <!-- Profile Dropdown -->
                    <div class="dropdown position-relative">
                        <button class="btn profile-dropdown-btn dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="https://png.pngtree.com/png-vector/20191110/ourmid/pngtree-avatar-icon-profile-icon-member-login-vector-isolated-png-image_1978396.jpg" 
                                 alt="Profile" class="profile-avatar">
                            <span class="profile-name d-none d-md-inline">
                                <%= (currentUser.profile && currentUser.profile.firstName && currentUser.profile.lastName) ? (currentUser.profile.firstName + ' ' + currentUser.profile.lastName) : currentUser.username %>
                            </span>
                        </button>
                        
                        <ul class="dropdown-menu dropdown-menu-end profile-dropdown-menu" aria-labelledby="profileDropdown">
                            <!-- User Info Section -->
                            <li class="profile-header">
                                <div class="d-flex align-items-center">
                                    <img src="https://png.pngtree.com/png-vector/20191110/ourmid/pngtree-avatar-icon-profile-icon-member-login-vector-isolated-png-image_1978396.jpg" 
                                         alt="Profile" class="profile-avatar-large me-3">
                                    <div>
                                        <div class="profile-dropdown-name">
                                            <%= (currentUser.profile && currentUser.profile.firstName && currentUser.profile.lastName) ? (currentUser.profile.firstName + ' ' + currentUser.profile.lastName) : currentUser.username %>
                                        </div>
                                        <div class="profile-dropdown-email">
                                            <%= currentUser.email %>
                                        </div>
                                        <div class="profile-dropdown-username">
                                            @<%= currentUser.username %>
                                        </div>
                                    </div>
                                </div>
                            </li>
                            
                            <!-- Profile Actions -->
                                <a class="dropdown-item profile-dropdown-item" href="/user/profile">
                                    <i class="fa-solid fa-user me-2"></i>
                                    My Profile
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item profile-dropdown-item" href="/user/wishlist">
                                    <i class="fa-solid fa-heart me-2"></i>
                                    My Wishlist
                                    <% if(currentUser.wishlist && currentUser.wishlist.length > 0) { %>
                                        <span class="badge bg-danger ms-2"><%= currentUser.wishlist.length %></span>
                                    <% } %>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item profile-dropdown-item" href="/booking/user/bookings">
                                    <i class="fa-solid fa-calendar-check me-2"></i>
                                    My Bookings
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item profile-dropdown-item" href="/user/my-listings">
                                    <i class="fa-solid fa-list me-2"></i>
                                    My Listings
                                </a>
                            </li>
                            
                            <li><hr class="dropdown-divider"></li>
                            
                            <!-- Logout -->
                            <li>
                                <a class="dropdown-item profile-dropdown-item text-danger" href="/user/logout">
                                    <i class="fa-solid fa-arrow-right-from-bracket me-2"></i>
                                    Logout
                                </a>
                            </li>
                        </ul>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<script src="/js/search.js"></script>

<style>
/* Navbar Profile Dropdown Styles */
.profile-dropdown-btn {
    background: linear-gradient(135deg, #ff385c, #ff6b6b);
    color: white;
    border: none;
    border-radius: 25px;
    padding: 0.5rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    transition: all 0.3s ease;
    box-shadow: 0 2px 10px rgba(255, 56, 92, 0.3);
}

.profile-dropdown-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 20px rgba(255, 56, 92, 0.4);
    color: white;
}

.profile-dropdown-btn:focus {
    box-shadow: 0 0 0 0.2rem rgba(255, 56, 92, 0.25);
    color: white;
}

.profile-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    border: 2px solid rgba(255, 255, 255, 0.3);
    object-fit: cover;
}

.profile-name {
    font-weight: 600;
    font-size: 0.95rem;
    color: white;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
}

.profile-dropdown-menu {
    background: white;
    border: none;
    border-radius: 1rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    min-width: 280px;
    margin-top: 1rem;
    padding: 0;
    overflow: hidden;
    transform: translateY(-10px);
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.profile-dropdown-menu.show {
    transform: translateY(0);
    opacity: 1;
    visibility: visible;
}

.profile-header {
    background: linear-gradient(135deg, #ff385c, #ff6b6b);
    color: white;
    padding: 1.5rem;
    margin: 0;
    border-radius: 1rem 1rem 0 0;
    position: relative;
}

.profile-header::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 0;
    border-left: 10px solid transparent;
    border-right: 10px solid transparent;
    border-top: 10px solid #ff385c;
}

.profile-avatar-large {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    border: 3px solid rgba(255, 255, 255, 0.4);
    object-fit: cover;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
}

.profile-dropdown-name {
    font-size: 1.2rem;
    font-weight: 700;
    color: white;
    margin-bottom: 0.25rem;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
}

.profile-dropdown-email {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 0.25rem;
}

.profile-dropdown-username {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.8);
    font-style: italic;
}

.profile-dropdown-item {
    padding: 0.875rem 1.5rem;
    color: #333;
    font-weight: 500;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    text-decoration: none;
}

.profile-dropdown-item:hover {
    background: linear-gradient(135deg, rgba(255, 56, 92, 0.1), rgba(255, 107, 107, 0.1));
    color: #ff385c;
    text-decoration: none;
    transform: translateX(5px);
}

.profile-dropdown-item i {
    width: 20px;
    text-align: center;
    margin-right: 0.75rem;
    color: #ff385c;
    transition: color 0.3s ease;
}

.profile-dropdown-item:hover i {
    color: #ff385c;
}

.dropdown-divider {
    margin: 0.5rem 0;
    border-color: rgba(255, 56, 92, 0.2);
}

/* Mobile responsiveness */
@media (max-width: 768px) {
    .profile-dropdown-menu {
        min-width: 240px;
        margin-right: 0.25rem;
        transform: translateX(-2px) translateY(10px);
        margin-top: 0.75rem;
    }

    .profile-dropdown-menu.show {
        transform: translateX(-2px) translateY(0);
    }

    .profile-header {
        padding: 1.25rem;
        text-align: center;
    }

    .profile-avatar-large {
        width: 50px;
        height: 50px;
        margin: 0 auto 0.75rem auto;
    }

    .profile-dropdown-name {
        font-size: 1.1rem;
    }

    .profile-dropdown-item {
        padding: 0.75rem 1.25rem;
    }
}

@media (max-width: 576px) {
    .profile-dropdown-menu {
        min-width: 220px;
        margin-right: 0.1rem;
    }

    .profile-header {
        padding: 1rem;
    }

    .profile-avatar-large {
        width: 45px;
        height: 45px;
    }

    .profile-dropdown-name {
        font-size: 1rem;
    }
}</style>