<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<% layout("/layouts/boilerplate") %>

    <body>

        <!-- <a href="http://localhost:8080/listing/new"><button>ADD NEW LOCATION</button></a> -->

        <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-5 ">
            <% for(let list of allListing) {%>
                <a href="/listing/<%= list._id %>" class="card-a-tag">

                    <div class="card col" style="width: 20rem;">
                        <img src="<%= list.image.url%>" class="card-img-top" alt="listing-image" style="height: 20rem;">
                            
                            <div class="card-img-overlay"></div>

                        <div class="card-body">
                            <p class="card-text">
                                <b>
                                    <%=list.title%>
                                </b> <br>
                                &#8377; <%= list.price.toLocaleString("en-IN") %>
                            </p>
                        </div>
                    </div>
                    <% } %>

                </a>
        </div>
    </body>

</html>